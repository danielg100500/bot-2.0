Это телеграм бот. Вместо надписи TOKEN нужно вставить токен, который вам сгенерировал Bot Father.
‼️ВАЖНО‼️
Чтобы узнать, что умеет зтот бот, нужно написать /info. Продублирую команды (/hello, /feel, /bye, /password, /1or2, /weather, /geo, /listwindows, /dance, /system, /system2, /random_system, /duck, /dog, /pokemon, /fox, /clean, а ещё он умеет повторять.)

В терминале 📟 нужно прописать:
wget https://github.com/OlafenwaMoses/ImageAI/releases/download/3.0.0-pretrained/yolov3.pt
pip install ImageAI
pipenv install pyTelegramBotAPI
pipenv install numpy
pipenv install keras
pipenv install requests
pipenv install tensorflow
pipenv install Pillow
